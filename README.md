**NOTE**

This repo is tailored towards my wants and needs. Go through all the files before
using anything.

`Xorg` with `dwm`. I don't use any patches.

`working` is the main branch. Somehow, I messed up `master`.
